-- V1 baseline for latex-specific Flyway location
-- This is an intentional no-op to establish version 1 in the migration-latex namespace.
-- Tables for LaTeX chat are created in the standard db/migration path; this stream holds
-- incremental adjustments and compatibility patches.

-- no-op, safe on all environments
SELECT 1;
